import { View, Text } from 'react-native'
import React from 'react'
import CustomText from './CustomText'

const Button = () => {
  return (
    <View>
      <CustomText classNames='text-2xl'>Button</CustomText>
    </View>
  )
}

export default Button